import {Button} from 'react-bootstrap'

function Container() {
    return (
        <main>
          <Button variant="success">SHERLY</Button>
        </main>
    );
  }
  
  export default Container;
  